# Groq API key (free tier — https://console.groq.com)
GROQ_API_KEY=

# HuggingFace token (optional, free — https://huggingface.co/settings/tokens)
HF_API_TOKEN=

# SQLite DB path (Railway: use /app/data/smartpaw.db or leave default)
# DB_PATH=data/smartpaw.db

# Upload directory for report images
# UPLOADS_DIR=uploads

# CORS origins — comma-separated (override default list if needed)
# CORS_ORIGINS=["https://your-app.vercel.app","http://localhost:3000"]
